# GitHub OAuth App credentials
# Create at: https://github.com/settings/developers → OAuth Apps → New OAuth App
# Authorization callback URL must be /api/auth/callback (NOT the PR webhook URL /api/webhooks/github).
GITHUB_CLIENT_ID=Ov23liGJthtoynhvjXqo
GITHUB_CLIENT_SECRET=774c7f678fd96af8f7717e76a98f7349bb8f0195
GITHUB_REDIRECT_URI=http://localhost:8080/api/auth/callback

# Database
DB_PASSWORD=architect

# JWT (change this in production - must be at least 32 characters)
JWT_SECRET=architect-secret-key-change-in-production-32bytes

# Application URLs
FRONTEND_URL=http://localhost:3000
VITE_API_BASE_URL=http://localhost:8080

# Optional: Slack webhook for notifications (Phase 2)
SLACK_WEBHOOK_URL=

# Optional: GitHub webhook secret for PR integration (Phase 2)
GITHUB_WEBHOOK_SECRET=

# Phase 3 — AI Understanding Layer
# Get your key at: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-...
